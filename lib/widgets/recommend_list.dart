import 'package:flutter/material.dart';

class RecommendListPage extends StatelessWidget {
  const RecommendListPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
